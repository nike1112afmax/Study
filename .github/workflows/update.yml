name: Update Dashboard Data

on:
  schedule:
    - cron: '0 0 * * 1-5'    # 台灣時間 08:00（UTC 00:00）
    - cron: '0 4 * * 1-5'    # 台灣時間 12:00（UTC 04:00）
    - cron: '30 13 * * 1-5'  # 台灣時間 21:30（UTC 13:30）
    - cron: '0 15 * * 1-5'   # 台灣時間 23:00（UTC 15:00）
    - cron: '0 17 * * 1-5'   # 台灣時間 01:00（UTC 17:00）
    - cron: '0 0 * * 6'     # 台灣時間 週六 08:00（UTC 00:00）
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch data
        run: python update_data.py

      - name: Commit and push data.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git diff --cached --quiet || git commit -m "chore: update dashboard data $(date +'%Y-%m-%d %H:%M')"
          git push
